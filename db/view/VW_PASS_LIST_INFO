CREATE OR REPLACE VIEW VW_PASS_LIST_INFO
SELECT USR.TX_FULL_NAME AS TX_USER_NAME,
	MUSR.TX_FULL_NAME AS TX_MOD_USER,
	PL.* FROM T_PASSWORD_LIST PL
	LEFT JOIN T_USER USR ON USR.ID_USER_KEY = PL.ID_USER_KEY
	LEFT JOIN T_USER MUSR ON MUSR.ID_USER_KEY = PL.ID_USER_MOD_KEY
ORDER BY PL.DTT_MOD DESC